<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.l03.projektpaszport">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MojeZdrowieActivity"
            android:label="@string/title_activity_moje_zdrowie"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".OMnieActivity"
            android:label="@string/title_activity_omnie"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".EdycjaOsobActivity" />
        <activity android:name=".DodajOsobeActivity" />
        <activity
            android:name=".PreferencjeActivity"
            android:label="@string/title_activity_preferencje"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>